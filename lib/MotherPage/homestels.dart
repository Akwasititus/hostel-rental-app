import 'package:flutter/material.dart';

class Homestels extends StatelessWidget {
  const Homestels({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Homstels')),
    );
  }
}